<div class="event group-<%= generalized_event_id(event, @friendEvents,
 @generalEvents, @friendID, @generalID) %>" id="event-<%= event.id %>" >
  <%= generalized_event_thumbnail(event, @friendEvents, @generalEvents, @friendID, @generalID) %>
  <% 
     start_day = event.start ? event.start.strftime("%a. %b. %e, %l:%M %p") : nil
     end_day = nil
	 if event.finish
		end_day = "-#{event.finish.strftime("%l:%M %p")}"
	 end
   %>
  <div class="title">
    <h4>
      <%= event.name %>
    </h4>
  </div>
  
  <div class="footer">
      <div class="links">
        <% if @favorites.include?(event) %>
          <%= link_to "Unfavorite", favorite_event_path(event), :class => "unfavorite", :remote => true %>
        <% else %>
          <%= link_to "Favorite", favorite_event_path(event), :class => "favorite", :remote => true %>
        <% end %>
        <div class='date right'><%= "#{start_day}#{end_day}" %></div>
      </div>
      <div class="location">
          <strong>Location:</strong>&nbsp;<%=event.location %>
      </div>
      <div class="group">
        From <%= generalized_event_name(event, @friendEvents, 
        @generalEvents , @friendID, @generalID, @groupNames) %>
      </div>
  </div>
  <div class="description">
    <%= event.description %>
  </div>
  <div class="info" style="display:none;">
    <% if !event.permalink.nil? %>
      <%= link_to "More Info", event.permalink, :target => "_blank" %>
    <% end %>
  </div>
</div>